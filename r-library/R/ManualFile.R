# This is not a generated file and should be left as is.
# The only problem is that it won't be exported properly as the library is not built with 
# Doxygen2. There would be an advantage is switching all the package man build to DOxygen2 annotations
# however this would require a working R environment with devtools installed for Java package compilation.
# I guess we could do both.

#' A very simple test
#'
#' @return a string
#' @export
manualFunction = function() {
  return("A test manual function")
}